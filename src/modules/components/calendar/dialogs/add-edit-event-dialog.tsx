"use client"

import type { ReactNode } from "react"

interface AddEditEventDialogProps {
  children: ReactNode
}

export function AddEditEventDialog({ children }: AddEditEventDialogProps) {
  return <>{children}</>
}
